---
---
<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-zinc-950">
  <!-- Atmosphere -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute -top-[25%] -left-[10%] w-[70%] h-[70%] bg-blue-500/10 rounded-full blur-[120px] animate-pulse"></div>
    <div class="absolute -bottom-[20%] -right-[10%] w-[60%] h-[60%] bg-purple-500/10 rounded-full blur-[120px] animate-pulse" style="animation-delay: 2s;"></div>
  </div>

  <div class="relative z-10 px-6 max-w-6xl mx-auto">
    <div class="flex flex-col items-center text-center">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 px-3 py-1 mb-10 rounded-full bg-zinc-900/50 border border-zinc-800 backdrop-blur-xl animate-fade-in">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-blue-500"></span>
        </span>
        <span class="text-xs font-medium text-zinc-400 tracking-wide uppercase">Available for new opportunities</span>
      </div>

      <!-- Main Heading -->
      <h1 class="text-5xl md:text-8xl font-bold tracking-tight mb-8 animate-slide-up">
        <span class="block text-zinc-100">Crafting Digital</span>
        <span class="block bg-clip-text text-transparent bg-gradient-to-r from-blue-400 via-indigo-400 to-purple-400 pb-2">
          Experiences for the Future
        </span>
      </h1>

      <p class="text-lg md:text-xl text-zinc-400 mb-12 max-w-2xl mx-auto leading-relaxed animate-slide-up opacity-0" style="animation-delay: 200ms;">
        I'm <span class="text-zinc-100 font-medium">Arjith Retna Srikanth</span>, a Senior Software Engineer at <span class="text-blue-400">Microsoft</span> building scalable, AI-driven solutions and high-performance mobile experiences.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center animate-slide-up opacity-0" style="animation-delay: 400ms;">
        <a href="#projects" class="px-8 py-3.5 bg-zinc-100 text-zinc-950 font-medium rounded-full transition-all hover:bg-white hover:scale-[1.02] active:scale-[0.98] shadow-lg shadow-white/5">
          Explore Projects
        </a>
        <a href="#contact" class="px-8 py-3.5 bg-zinc-900 text-zinc-100 font-medium rounded-full border border-zinc-800 transition-all hover:bg-zinc-800 hover:scale-[1.02] active:scale-[0.98]">
          Get in Touch
        </a>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-12 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 text-zinc-500 animate-fade-in" style="animation-delay: 1s;">
      <span class="text-[10px] uppercase tracking-[0.2em] font-medium">Scroll to explore</span>
      <div class="w-[1px] h-12 bg-gradient-to-b from-blue-500/50 to-transparent"></div>
    </div>
  </div>
</section>

<style>
  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }
  .animate-slide-up {
    animation: slideUp 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes slideUp {
    from { opacity: 0; transform: translateY(40px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style>
